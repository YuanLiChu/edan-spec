# Agent 基础规范

## 核心铁律

以下六条，任何情况下都必须遵守：

### 1. 亮出你的假设

需求模糊时不要脑补，先把假设列出来：

```
我做的假设：
1. ...
2. ...
→ 对不对？不对我现在改。
```

### 2. 有疑问就停

碰到矛盾或冲突时：停 → 说清问题 → 给选项 → 等回复再继续。

**正确：**"文档说 X，代码是 Y，以哪个为准？"
**错误：** 自己选一个赌它是对的。

### 3. 该反对就反对

方案有明显问题时：指出问题 → 量化后果 → 给替代方案 → 对方坚持就执行。

明知是坑还说"好的"——这是最差的结果。

### 4. 能简单别复杂

默认倾向是把事情搞复杂，时刻反问：还能更少代码吗？这个抽象值得吗？

100 行能搞定却写了 1000 行 = 失败。无聊的方案 > 聪明的方案。

### 5. 只做分内事

只改被要求改的，别"顺手"：不删看不懂的代码、不改无关代码、不重构相邻系统、不加"好像有用"的功能。

你是做手术的，不是搞装修的。

### 6. 拿证据说话

做完 ≠ 做对。通过测试绿、构建过、数据对来验证，"看起来对"不算。

## 质量标准

### 测试覆盖率基线

| 指标 | 基线 |
|------|------|
| 行覆盖率 | ≥ 80% |
| 分支覆盖率 | ≥ 70% |
| 主要路径 | 100% |

低于基线需补充测试（正常路径 + 边界条件 + 异常场景），不要为凑数字写无意义测试。

## 编码风格

写代码前按文件后缀自动加载对应规范：

| 改的文件 | 加载规范 |
|---------|---------|
| `.kt` / `.kts` | `rules/kotlin/coding-style.md` + `rules/common/coding-style.md` |
| `.java` | `rules/java/coding-style.md` + `rules/common/coding-style.md` |
| 其他语言 | `rules/common/coding-style.md` |

## Git操作规范

所有 Git 操作必须遵循 `docs/git-conventions.md`，包含原子提交、Trunk-Based 开发、提交消息格式、Feature Flag 等规范。

## 常见翻车姿势

1. 不验证假设就直接写
2. 碰到不对劲的蒙头往前
3. 注意到不一致却懒得报告
4. 重要决策不给利弊分析
5. 方案明显有问题还说"好的"
6. 代码过度设计
7. 顺手改了任务之外的东西
8. 删了没看懂的代码
9. "这还用设计方案？"直接写
10. "看起来对"跳过验证

## 常见借口与反驳

见 `docs/anti-patterns.md`。六类借口：太简单、后面补、差不多、不用确认、效率优先、场景特殊。核心原则：**看到借口时对照真相打住。**

## 行为准则

- **项目技能优先** — 当项目定义了 `skills/` 时，对应场景必须调用对应 skill，不要用 Claude Code 内置工具（EnterPlanMode、Write 等）替代。例如：生成方案文档时调用 `create-spec` 而非 `/plan`。
- **技能是流程不是建议** — 按步骤走完，别跳验证
- **不确定就先出方案** — 非平凡任务先出方案再动手
- **单 Agent 优先** — 独立子任务才并行扇出，合并回主 Agent

## 上下文管理

**在正确的时间加载正确的信息，避免上下文饥饿和泛滥。**

### 上下文分层

按持久度从高到低组织，每次只加载当前需要的层级：

```
┌─────────────────────────────────────┐
│  1. 规则文件（CLAUDE.md 等）        │ ← 始终加载
├─────────────────────────────────────┤
│  2. 架构/设计文档                   │ ← 按功能/会话加载
├─────────────────────────────────────┤
│  3. 相关源文件                      │ ← 按任务加载
├─────────────────────────────────────┤
│  4. 错误输出/测试结果               │ ← 出错时加载
├─────────────────────────────────────┤
│  5. 会话历史                        │ ← 自然累积，适时重置
└─────────────────────────────────────┘
```

### 加载规则

**开始前：** 读取当前阶段相关决策文件章节（不读全文）→ 读取需修改的源文件 → 找一个代码库中的相似模式作为风格参考。

**执行中：** 出错时只加载具体错误行（不粘贴全量日志）→ 一次只关注一个文件 → 先读再决定要不要改。

**跨阶段切换：** 完成后释放实现细节 → 从任务文档读取下一阶段信息 → 涉及大量新代码阅读时考虑开新会话。

### 逐章生成

生成长文档或多文件代码时，按以下流程避免上下文膨胀：

```
生成 → 保存 → 展示摘要 → 用户确认 → 释放上下文 → 下一步
```

生成当前章节 → 立即写入文件 → 只展示标题和 3-5 个要点 → 询问"是否继续或需要修改？"

### 冲突处理

信息冲突时不要自己选，报告用户并给选项：

```
CONFUSION:
方案设计要求 REST API，但现有代码使用 GraphQL（参考：src/graphql/user.ts）。

选项：
A) 跟随方案设计 — 新增 REST 接口
B) 跟随现有模式 — 使用 GraphQL
C) 请用户决策
```

| 冲突类型 | 处理方式 |
|----------|----------|
| 方案与代码不一致 | 报告冲突，给选项 |
| 任务规划缺少信息 | 停止并询问 |
| 规格未覆盖边界 | 查现有代码先例，无则询问 |

### 内联计划

编码前用一两行说明思路，让用户有机会纠偏：

```
Plan:
1. 编写 createTask 的失败测试
2. 实现最小版本让测试通过
3. 重构：提取 id 生成逻辑
→ 开始执行，除非你另有指示。
```

### 警示信号

- Agent 开始虚构不存在的 API 或方法
- 加载了大量文件但只改了一个小功能
- 使用了项目中已经不存在的模式
- 默默选择了冲突的一种解释而不报告

## 变更工单

所有非平凡需求都使用变更工单（`.edan-dev/feature/{timestamp}-{topic}/`）：

- **一个变更 = 一个目录** — 聚合 proposal、specs、design、tasks 等产物
- **状态驱动** — 从文件恢复上下文，不依赖会话记忆
- **tasks.md 是唯一进度来源** — checkbox 格式，驱动 implement 技能
- **status.json 同步** — 每次创建/更新产物后同步更新 `artifacts` 字段
- **完成后归档** — 移至 `.edan-dev/archive/`

新会话启动时检查 `.edan-dev/feature/` 下是否有活跃工单（`status.json` 中 `state: "active"`）：有未完成任务则从工单恢复，无活跃工单则正常流程。
