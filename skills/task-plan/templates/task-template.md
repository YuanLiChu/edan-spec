# 任务清单 — {功能名称}

## 概述

{一句话描述本 feature 的目标和范围。不超过 3 行。}

---

## 架构决策

- 技术栈：{框架/语言}
- 架构模式：{模式}
- 关键决策：{为什么选 A 不选 B}

---

## 任务

### Task-001：{任务标题}

**描述**：{做什么、为什么做、交付什么}
**关联需求**：{对应 specs/ 或 proposal.md 的位置}
**前置依赖**：{前置任务ID 或 无}

**工时估算**：{总计} 人天
- 基础：{X} 人天（XS/S/M）
- 缓冲：{X} 人天（{风险类型}-{等级}）
- 总计：{X} 人天
→ Agent：缓冲计算规则见 `references/risk-classification.md`。

**涉及文件**：
- `{路径}` — {用途}（{新增/修改}）

**验收标准**：
- **{标准名}**：当 {条件} 时，{结果}。验证方式：`{测试命令}` 通过 && 覆盖率报告中新增文件行覆盖率 ≥80%、分支覆盖率 ≥70%。

**增量计划**：
- [ ] **增量 1**：{名称}
  - 做什么：{内容}
  - 交付：{可验证结果}
  - 对应验收标准：{标准名}
  - 完成判定：`{测试命令}` 通过 && `{构建命令}` 无错误 && 覆盖率达标（新增文件行 ≥80%、分支 ≥70%，使用 `{覆盖率检查命令}` 验证）

**失败策略**：同一增量修复超 2 次 → 停下调 meddev:debugging；超 3 次 → 停下调 meddev:explore；超 4 次 → 停止并报告用户。

---

{更多任务按相同格式继续}

---

### Task-{N}：{检查点名称} 检查点

**类型**：检查点
**触发条件**：{哪些任务完成后触发}

**验证项**（仅包含可自动验证项）：
- [ ] 构建：`{命令}` 无错误
- [ ] 测试：`{命令}` 全部通过
- [ ] 覆盖率：使用 `{覆盖率命令}` 生成报告，并用 `{阈值检查命令}` 验证新增文件行覆盖率 ≥80%、分支覆盖率 ≥70%
- [ ] 任务完整性：所有相关 Task 增量 checkbox 全勾
- [ ] 依赖变更：无未声明的新增第三方依赖

→ Agent：检查点作为 Task 类别参与统一编号（如 Task-003: Domain 层完成检查点），与开发任务按实施顺序交替排列。检查项必须是可自动执行的命令/断言，禁止出现"手动验证"、"人工确认"、"回归测试"等不可自动验证的条目。覆盖率检查必须使用带阈值断言的命令（如 `gcovr --fail-under-line 80 --fail-under-branch 70` 或解析 `llvm-cov` 报告输出断言覆盖率数值），不能仅生成报告不做校验。

---

{更多任务或检查点按相同格式继续}

---

## 风险

| 风险 | 等级 | 应对 |
|------|------|------|
| {描述} | {低/中/高} | {策略} |

---

## 实施顺序

### 阶段一：{名称}

| 任务 | 耗时 | 前置依赖 | 执行方式 |
|------|------|----------|----------|
| Task-001：{标题} | {X}h | 无 | 串行 |

### 阶段二：{名称}

<!-- PARALLEL: Task-003, Task-004 -->

| 任务 | 耗时 | 前置依赖 | 执行方式 |
|------|------|----------|----------|
| Task-003：{标题} | {X}h | Task-001 | **可并行**（{具体依据}） |
| Task-004：{标题} | {X}h | Task-001 | **可并行**（{具体依据}） |

**关键路径**：{计算过程}
**总工期**：{关键路径之和} 人天

→ Agent：并行前确认：1）前置全部 done；2）文件无交集；3）可正常编译。
